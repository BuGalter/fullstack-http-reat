import React from 'react';
import '../../styles/calendar.css';

const CalendarButton = ({ text }) => {
  return <button className='calendar-button'>{text}</button>;
};

export default CalendarButton;
